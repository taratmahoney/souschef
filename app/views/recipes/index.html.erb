<% @recipe.each do |recipe| %>
  <h4><%= link_to recipe.name, "/recipes/#{recipe.id}" %> </h4>
  <img class="recipe-thumb" src="<%= recipe.image %>" />
<% end %>

<% if current_user %>
  <%= link_to "Add Recipe", new_recipe_path %>
<% end %>