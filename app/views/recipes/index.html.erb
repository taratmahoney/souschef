
<div class="landing">
<h1> Hey! Welcome to <span> souschef. </span> </h1>
<h2> Select a recipe below to get started, or <%= link_to "add your own here.", new_recipe_url %> </h2>
</div>


<div class="recipes">
  <% @recipe.each do |recipe| %>

    <div class="recipe-list-item">
      <div class="recipe-list-image">
        <%= image_tag(recipe.image) %>
      </div>

      <div class="recipe-list-name">
        <h4><%= link_to recipe.name, "/recipes/#{recipe.id}" %> </h4>
      </div>
    </div>

<% end %>

